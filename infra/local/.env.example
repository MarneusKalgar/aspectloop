# AspectLoop local infrastructure environment template
# Copy to .env.local for the local Compose stack and adjust values if needed.

# PostgreSQL host and bootstrap administrator. The administrator is local-only
# and must not be used by application services.
POSTGRES_PORT=5432
POSTGRES_ADMIN_USER=aspectloop_admin
POSTGRES_ADMIN_PASSWORD=aspectloop_admin
POSTGRES_ADMIN_DATABASE=postgres

# Platform separates schema ownership from its two long-lived runtime roles.
PLATFORM_DATABASE_NAME=platform_db
PLATFORM_MIGRATOR_USER=platform_migrator
PLATFORM_MIGRATOR_PASSWORD=platform_migrator
PLATFORM_RUNTIME_USER=platform_runtime
PLATFORM_RUNTIME_PASSWORD=platform_runtime
GATEWAY_CORRECTION_RUNTIME_USER=gateway_correction_runtime
GATEWAY_CORRECTION_RUNTIME_PASSWORD=gateway_correction_runtime
# Extraction and correction still use one owner/runtime role until their
# corresponding ownership milestones.
EXTRACTION_DATABASE_NAME=extraction_db
EXTRACTION_DATABASE_USER=extraction_app
EXTRACTION_DATABASE_PASSWORD=extraction_app
CORRECTION_DATABASE_NAME=correction_db
CORRECTION_DATABASE_USER=correction_app
CORRECTION_DATABASE_PASSWORD=correction_app

# Local connection budget: 100 total, 3 superuser-reserved, 30 for one replica
# of each runtime, 6 for bounded tools, and at least 20 operational headroom.
POSTGRES_MAX_CONNECTIONS=100
POSTGRES_SUPERUSER_RESERVED_CONNECTIONS=3
POSTGRES_OPERATIONAL_HEADROOM=20
POSTGRES_TOOL_CONNECTION_BUDGET=6
GATEWAY_DB_POOL_SIZE=5
PLATFORM_DB_POOL_SIZE=5
EXTRACTION_DB_POOL_SIZE=10
CORRECTION_DB_POOL_SIZE=10

# pgAdmin is opt-in local developer tooling. Keep its UI loopback-only and
# replace the password placeholder in the ignored local environment file.
PGADMIN_PORT=5050
PGADMIN_DEFAULT_EMAIL=admin@aspectloop.dev
PGADMIN_DEFAULT_PASSWORD=replace-with-local-only-password
# The owner/migrator registration is hidden unless explicitly requested.
PGADMIN_INCLUDE_PLATFORM_MIGRATOR=false

# Garage uses one local node, the fixed garage region, and distinct private
# credentials per bucket owner.
# The initializer replaces only these exact placeholders in ignored .env.local.
GARAGE_RPC_SECRET=generate-with-local-garage-init
# Host-side bootstrap URL address; it must remain loopback-only.
GARAGE_HOST=127.0.0.1
GARAGE_S3_PORT=3900
GARAGE_ADMIN_PORT=3903
GARAGE_ZONE=local
GARAGE_CAPACITY_BYTES=1000000000
PLATFORM_S3_BUCKET=aspectloop-platform-source
PLATFORM_S3_ACCESS_KEY_ID=generate-with-local-garage-init
PLATFORM_S3_SECRET_ACCESS_KEY=generate-with-local-garage-init
EXTRACTION_S3_BUCKET=aspectloop-extraction-artifacts
EXTRACTION_S3_ACCESS_KEY_ID=generate-with-local-garage-init
EXTRACTION_S3_SECRET_ACCESS_KEY=generate-with-local-garage-init
CORRECTION_S3_BUCKET=aspectloop-correction-artifacts
CORRECTION_S3_ACCESS_KEY_ID=generate-with-local-garage-init
CORRECTION_S3_SECRET_ACCESS_KEY=generate-with-local-garage-init
