scalar DateTime
"""
Arbitrary JSON value. Prefer GraphQL variables or raw object literals instead of pre-stringifying payloads.
"""
scalar JSON

type Query {
  _empty: String
}

type Mutation {
  _empty: String
}