/** Credentials for the default account exposed by the browser mock runtime. */
export const defaultMockReviewerCredentials = {
  email: 'reviewer@aspectloop.test',
  password: 'password123',
} as const;
